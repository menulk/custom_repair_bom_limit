<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_repair_order_form_inherit_product_filter" model="ir.ui.view">
        <field name="name">repair.order.form.product.filter</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_form"/>
        <field name="arch" type="xml">
            <field name="product_id" position="attributes">
                <attribute name="context">{'from_repair_order': 1}</attribute>
            </field>
        </field>
    </record>   
    <record id="view_stock_move_inherit_product_filter" model="ir.ui.view">
        <field name="name">stock.move.form.product.filter</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form"/>
        <field name="arch" type="xml">
            <field name="product_id" position="attributes">
                <attribute name="context">{'default_detailed_type': 'product'}</attribute>
            </field>
        </field>
    </record>
</odoo>
